@model Biblioteca.Models.CardItem

<div class="lib-card">
    <div class="lib-card-icon">@Model.Icono</div>
    <h3 class="lib-card-title">@Model.Titulo</h3>
    <p class="lib-card-desc">@Model.Descripcion</p>
</div>
